<h1>New Article</h1>

<%= form_for @article do |a| %>
	<%= a.label :title %>
	<%= a.text_field :title %>
	<br>

	<%= a.label :body %>
	<%= a.text_field :body %>
	<br>

	<%= a.submit "Create Article" %>

	<% end %>	